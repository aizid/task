package com.example.tasks.app.data.mapper

/*
import com.example.tasks.app.base.mapper.BaseMapper
import com.example.tasks.app.domain.dto.response.source.Source
import com.example.tasks.app.domain.entities.SourceModel

class SourceMapper: BaseMapper<Source?, SourceModel>() {

    override fun map(value: Source?): SourceModel {
        value?.let {
            return SourceModel(
                it.id
            )
        }
        return SourceModel("")
    }
}*/
